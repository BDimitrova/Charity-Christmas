<section id="detailsPage">
    <div id="detailsInfo">
        <h1>Title: {{title}}</h1>
        <div class="info"><img src="{{image}}"></div>
        <div class="info">
            <h3>CHARITY: {{charity}}</h3>
            <h3>CATEGORY: {{category}}</h3>
            <h3>DESCRIPTION: {{description}}</h3>
            <h2>PRICE: ${{price}}.00</h2>
        </div>
        {{#if user}}
        <div class="buttons">
            {{#if isOwner}}
            <a href="/{{_id}}/edit" class="edit-btn">Edit</a>
            <a href="/{{_id}}/delete" class="delete-btn">Delete</a>
            {{else if isBought}}
            <p><span class="buy">Thank You For Your Purchase :)</span></p>
            {{else}}
            <a href="/{{_id}}/buy" class="buy-btn">Buy</a>
            {{/if}}
        </div>
        {{/if}}
    </div>
</section>